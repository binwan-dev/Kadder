using System.Reflection;

namespace Atlantis.Grpc
{
    public class GrpcMethodCheck
    {
        public void CheckRegService(MethodInfo method)
        {
            
        }

        private void CheckMethod(MethodInfo method)
        {
            if(method.ReturnType.)
        } 
    }
}